# nvim runtime under XDG_DATA_HOME / STATE / CACHE
.data/
.state/
.cache/
.claude/settings.local.json
.claude/settings.tmp.json

# user-local cterm config (created by setup.py)
cterm.local
cterm.local.cmd

# user-local nvim overrides
nvim/lua/config/local.lua

# local-only superpowers workspace
docs/superpowers/

# OS / editor cruft
Thumbs.db
.DS_Store
*.swp
